services:
  - type: web
    name: robot-vacuum-viewer
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python robot_viewer/server.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
